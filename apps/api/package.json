{
  "name": "@oshi/api",
  "private": true,
  "scripts": {
    "dev": "node run-venv.mjs -m uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "start": "node run-venv.mjs -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "test": "node run-venv.mjs -m pytest tests -q"
  }
}
