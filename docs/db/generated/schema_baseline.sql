-- GENERATED by scripts/generate_db_docs.py — 手編集禁止。 再生成: python scripts/generate_db_docs.py 

-- GENERATED schema baseline (documentation / disaster reference)
-- 既存プロジェクトへの再適用用ではない。正の変更は supabase/migrations/ へ。
-- generated_at_utc: 2026-09-04T08:40:30.403150+00:00

-- === category_tag ===
CREATE TABLE IF NOT EXISTS public.category_tag (
  category_tag_id integer NOT NULL DEFAULT nextval('category_tag_category_tag_id_seq'::regclass),
  members_id uuid NOT NULL,
  category_tag_color text,
  category_tag_name text NOT NULL,
  category_tag_icon text,
  category_tag_use_flag integer DEFAULT 1,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  slot integer,
  display_order integer NOT NULL DEFAULT 0
);

-- === category_tag_preset_slot_dismissed ===
CREATE TABLE IF NOT EXISTS public.category_tag_preset_slot_dismissed (
  members_id uuid NOT NULL,
  slot integer NOT NULL,
  dismissed_at timestamp with time zone NOT NULL DEFAULT now()
);

-- === character ===
CREATE TABLE IF NOT EXISTS public."character" (
  character_id integer NOT NULL DEFAULT nextval('character_character_id_seq'::regclass),
  work_id integer,
  works_series_id integer,
  theme_color integer,
  hair_color integer,
  eye_color integer,
  character_name text,
  nickname text,
  sex text,
  person_flag integer DEFAULT 0,
  animal_flag integer DEFAULT 0,
  existing_flag integer DEFAULT 0,
  foot_number integer,
  height integer,
  weight integer,
  birthday date,
  debut_date date,
  age integer,
  student_flag integer DEFAULT 0,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === color ===
CREATE TABLE IF NOT EXISTS public.color (
  color_group_id integer NOT NULL DEFAULT nextval('color_color_group_id_seq'::regclass),
  color_group_name text NOT NULL,
  color_preference text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === color_tag ===
CREATE TABLE IF NOT EXISTS public.color_tag (
  color_tag_id integer NOT NULL DEFAULT nextval('color_tag_color_tag_id_seq'::regclass),
  members_id uuid NOT NULL,
  color_tag_color text,
  color_tag_name text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  slot integer NOT NULL
);

-- === copyright_company ===
CREATE TABLE IF NOT EXISTS public.copyright_company (
  copyright_company_id integer NOT NULL DEFAULT nextval('copyright_company_copyright_company_id_seq'::regclass),
  copyright_company_name text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === currency_unit ===
CREATE TABLE IF NOT EXISTS public.currency_unit (
  currency_unit_id integer NOT NULL DEFAULT nextval('currency_unit_currency_unit_id_seq'::regclass),
  currency_name text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === data_export ===
CREATE TABLE IF NOT EXISTS public.data_export (
  data_export_id uuid NOT NULL DEFAULT gen_random_uuid(),
  members_id uuid NOT NULL,
  kind text NOT NULL,
  status text NOT NULL DEFAULT 'pending'::text,
  storage_path text,
  error_code text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  expires_at timestamp with time zone NOT NULL
);

-- === display_settings ===
CREATE TABLE IF NOT EXISTS public.display_settings (
  members_id uuid NOT NULL,
  members_type_name text NOT NULL DEFAULT 'default'::text,
  text_scale smallint NOT NULL DEFAULT 3,
  ui_density smallint NOT NULL DEFAULT 4,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now(),
  list_sort text NOT NULL DEFAULT 'newest'::text,
  gallery_layout text NOT NULL DEFAULT 'grid'::text,
  landing_page text NOT NULL DEFAULT 'home'::text,
  residence_region text NOT NULL DEFAULT 'jp'::text,
  timezone_override text,
  date_format_mode text NOT NULL DEFAULT 'residence'::text,
  currency_code_override text,
  currency_format_mode text NOT NULL DEFAULT 'residence'::text,
  register_start_step text NOT NULL DEFAULT 'barcode'::text,
  default_storage_location_id integer,
  gallery_show_name boolean NOT NULL DEFAULT true,
  gallery_show_tags boolean NOT NULL DEFAULT true,
  gallery_show_price boolean NOT NULL DEFAULT true
);

-- === gallery_view ===
CREATE TABLE IF NOT EXISTS public.gallery_view (
  gallery_view_id bigint NOT NULL,
  members_id uuid NOT NULL,
  view_name text NOT NULL,
  q text,
  category_tag_ids integer[] NOT NULL DEFAULT '{}'::integer[],
  storage_location_ids integer[] NOT NULL DEFAULT '{}'::integer[],
  color_tag_slots integer[] NOT NULL DEFAULT '{}'::integer[],
  list_sort text NOT NULL DEFAULT 'newest'::text,
  display_order integer NOT NULL DEFAULT 0,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now()
);

-- === icon_tag ===
CREATE TABLE IF NOT EXISTS public.icon_tag (
  icon text NOT NULL,
  icon_name text NOT NULL,
  category_tag_use_flag integer DEFAULT 1,
  storage_location_use_flag integer DEFAULT 1
);

-- === member ===
CREATE TABLE IF NOT EXISTS public.member (
  members_id uuid NOT NULL,
  members_type_name text,
  user_name text,
  email_address text,
  x_id text,
  instagram_id text,
  line_id text,
  identity_type text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === member_type ===
CREATE TABLE IF NOT EXISTS public.member_type (
  members_type_name text NOT NULL,
  thumbnail_image_quality integer DEFAULT 80,
  registerable_number integer DEFAULT 100,
  high_resolution_registerable_number integer DEFAULT 10,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === oshi_accent_settings ===
CREATE TABLE IF NOT EXISTS public.oshi_accent_settings (
  members_id uuid NOT NULL,
  members_type_name text NOT NULL DEFAULT 'default'::text,
  main_hex text NOT NULL DEFAULT '#9f606c'::text,
  sub_hex text NOT NULL DEFAULT '#6a9bb8'::text,
  active boolean NOT NULL DEFAULT false,
  presets jsonb NOT NULL DEFAULT '[]'::jsonb,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  updated_at timestamp with time zone NOT NULL DEFAULT now()
);

-- === photo ===
CREATE TABLE IF NOT EXISTS public.photo (
  photo_id integer NOT NULL DEFAULT nextval('photo_photo_id_seq'::regclass),
  members_id uuid NOT NULL,
  photo_theme_color integer,
  front_flag integer DEFAULT 0,
  photo_thumbnail bytea,
  photo_thumbnail_image_quality integer DEFAULT 80,
  photo_high_resolution_flag integer DEFAULT 0,
  photo_edited_flag integer DEFAULT 0,
  photo_registration_date timestamp with time zone DEFAULT now(),
  photo_edit_date timestamp with time zone,
  photo_thumbnail_url text,
  photo_high_resolution_url text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === product_size ===
CREATE TABLE IF NOT EXISTS public.product_size (
  product_size_id integer NOT NULL DEFAULT nextval('product_size_product_size_id_seq'::regclass),
  product_group_id integer,
  product_type text NOT NULL,
  product_size_horizontal integer,
  product_size_depth integer,
  product_size_vertical integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === product_type ===
CREATE TABLE IF NOT EXISTS public.product_type (
  product_group_id integer NOT NULL DEFAULT nextval('product_type_product_group_id_seq'::regclass),
  product_group_name text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === registered_product ===
CREATE TABLE IF NOT EXISTS public.registered_product (
  registered_product_id integer NOT NULL DEFAULT nextval('registered_product_registered_product_id_seq'::regclass),
  members_id uuid NOT NULL,
  photo_id integer,
  works_series_id integer,
  work_id integer,
  character_id integer,
  copyright_company_id integer,
  product_group_id integer,
  product_size_id integer,
  storage_location_id integer,
  storage_location_tag_id integer,
  color_tag_id integer,
  category_tag_id integer,
  campaign_id integer,
  currency_unit_id integer,
  works_series_name text,
  title text,
  character_name text,
  copyright_company_name text,
  product_type text,
  product_size_horizontal integer,
  product_size_depth integer,
  product_size_vertical integer,
  barcode_number text,
  barcode_type text,
  product_name text,
  list_price integer,
  purchase_price integer,
  registration_quantity integer DEFAULT 1,
  sales_desired_quantity integer,
  product_series_quantity integer,
  purchase_location text,
  freebie_name text,
  purchase_date date,
  creation_date timestamp with time zone DEFAULT now(),
  updated_date timestamp with time zone DEFAULT now(),
  other_tag text,
  memo text,
  product_series_flag integer DEFAULT 0,
  commercial_product_flag integer DEFAULT 1,
  personal_product_flag integer DEFAULT 0,
  digital_product_flag integer DEFAULT 0,
  sales_desired_flag integer DEFAULT 0,
  want_object_flag integer DEFAULT 0,
  freebie_flag integer DEFAULT 0,
  product_series_complete_flag integer DEFAULT 0,
  product_group_name text,
  currency_code text
);

-- === registered_product_color_tag ===
CREATE TABLE IF NOT EXISTS public.registered_product_color_tag (
  members_id uuid NOT NULL,
  registered_product_id integer NOT NULL,
  slot integer NOT NULL
);

-- === storage_location ===
CREATE TABLE IF NOT EXISTS public.storage_location (
  storage_location_id integer NOT NULL DEFAULT nextval('storage_location_storage_location_id_seq'::regclass),
  storage_location_name text NOT NULL,
  storage_location_size_horizontal integer,
  storage_location_size_depth integer,
  storage_location_size_vertical integer,
  storage_count_per_1 integer DEFAULT 1,
  storage_size_horizontal_per_1 integer,
  storage_size_depth_per_1 integer,
  storage_size_vertical_per_1 integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  storage_location_icon text,
  storage_location_use_flag integer DEFAULT 1,
  members_id uuid NOT NULL,
  slot integer,
  display_order integer NOT NULL DEFAULT 0,
  register_pick_count integer NOT NULL DEFAULT 0,
  last_register_picked_at timestamp with time zone
);

-- === storage_location_preset_slot_dismissed ===
CREATE TABLE IF NOT EXISTS public.storage_location_preset_slot_dismissed (
  members_id uuid NOT NULL,
  slot integer NOT NULL,
  dismissed_at timestamp with time zone NOT NULL DEFAULT now()
);

-- === theme_settings ===
CREATE TABLE IF NOT EXISTS public.theme_settings (
  members_id uuid NOT NULL,
  members_type_name text NOT NULL,
  theme text NOT NULL DEFAULT 'default'::text,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- === work ===
CREATE TABLE IF NOT EXISTS public.work (
  work_id integer NOT NULL DEFAULT nextval('work_work_id_seq'::regclass),
  title text NOT NULL,
  works_series_id integer,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  copyright_company_name text
);

-- === works_series ===
CREATE TABLE IF NOT EXISTS public.works_series (
  works_series_id integer NOT NULL DEFAULT nextval('works_series_works_series_id_seq'::regclass),
  works_series_name text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now()
);

-- indexes / constraints: see constraints.md（生成）と migrations
