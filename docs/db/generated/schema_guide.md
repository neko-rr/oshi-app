<!-- GENERATED by scripts/generate_db_docs.py — 手編集禁止。 再生成: python scripts/generate_db_docs.py -->
# 生成: スキーマ読み下し（日本語）

コード・SQL は英語物理名。**このファイルは意味の説明用**（自動生成）。

生成日時(UTC): `2026-09-04 08:40:30Z`

## 使い方

- エージェントはユーザーに表・列を説明するとき **ここを正** にする
- プレビューで図を見る: [er-diagram.md](er-diagram.md)（`Ctrl+Shift+V`）
- 詳細型・制約: [columns.md](columns.md) / [constraints.md](constraints.md)

## カテゴリータグ（`category_tag`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 10

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| カテゴリータグID | `category_tag_id` | `integer` | 識別子または外部キー |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| カテゴリータグ色 | `category_tag_color` | `text` |  |
| カテゴリータグ名 | `category_tag_name` | `text` |  |
| カテゴリータグアイコン | `category_tag_icon` | `text` |  |
| カテゴリータグ使用フラグ | `category_tag_use_flag` | `integer` | 真偽・0/1 フラグ |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| スロット | `slot` | `integer` | 枠番号（プリセット等） |
| 表示順 | `display_order` | `integer` | 表示並び順 |

## カテゴリプリセット削除済（`category_tag_preset_slot_dismissed`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 3

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| スロット | `slot` | `integer` | 枠番号（プリセット等） |
| 却下日時 | `dismissed_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## キャラクター（`character`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 21

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| キャラクターID | `character_id` | `integer` | 識別子または外部キー |
| 作品ID | `work_id` | `integer` | 識別子または外部キー |
| 作品シリーズID | `works_series_id` | `integer` | 識別子または外部キー |
| テーマ色 | `theme_color` | `integer` |  |
| 髪色 | `hair_color` | `integer` |  |
| 瞳色 | `eye_color` | `integer` |  |
| キャラクター名 | `character_name` | `text` |  |
| ニックネーム | `nickname` | `text` |  |
| 性別 | `sex` | `text` |  |
| 人物フラグ | `person_flag` | `integer` | 真偽・0/1 フラグ |
| 動物フラグ | `animal_flag` | `integer` | 真偽・0/1 フラグ |
| 既存フラグ | `existing_flag` | `integer` | 真偽・0/1 フラグ |
| 足数 | `foot_number` | `integer` |  |
| 身長 | `height` | `integer` |  |
| 重量 | `weight` | `integer` |  |
| 誕生日 | `birthday` | `date` |  |
| デビュー日 | `debut_date` | `date` | 日付 |
| 年齢 | `age` | `integer` |  |
| 学生フラグ | `student_flag` | `integer` | 真偽・0/1 フラグ |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 色（`color`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 5

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 色グループID | `color_group_id` | `integer` | 識別子または外部キー |
| 色グループ名 | `color_group_name` | `text` |  |
| 色の好み | `color_preference` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## カラータグ（`color_tag`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 7

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| カラータグID | `color_tag_id` | `integer` | 識別子または外部キー |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| カラータグ色 | `color_tag_color` | `text` |  |
| カラータグ名 | `color_tag_name` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| スロット | `slot` | `integer` | 枠番号（プリセット等） |

## 版権会社（`copyright_company`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 4

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 版権会社ID | `copyright_company_id` | `integer` | 識別子または外部キー |
| 版権会社名 | `copyright_company_name` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 貨幣単位（`currency_unit`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 4

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 貨幣単位ID | `currency_unit_id` | `integer` | 識別子または外部キー |
| 通貨名 | `currency_name` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## データ書き出し（`data_export`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 9

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| データ書き出しID | `data_export_id` | `uuid` | 識別子または外部キー |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 種別 | `kind` | `text` |  |
| 状態 | `status` | `text` |  |
| ストレージパス | `storage_path` | `text` |  |
| エラーコード | `error_code` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 失効日時 | `expires_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 表示設定（`display_settings`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 19

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 会員種別名 | `members_type_name` | `text` |  |
| 文字倍率 | `text_scale` | `smallint` |  |
| UI密度 | `ui_density` | `smallint` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 一覧並び | `list_sort` | `text` |  |
| ギャラリー表示 | `gallery_layout` | `text` |  |
| 着地ページ | `landing_page` | `text` |  |
| 居住地域 | `residence_region` | `text` |  |
| タイムゾーン上書き | `timezone_override` | `text` |  |
| 日付形式モード | `date_format_mode` | `text` |  |
| 通貨コード上書き | `currency_code_override` | `text` |  |
| 金額形式モード | `currency_format_mode` | `text` |  |
| 登録開始手順 | `register_start_step` | `text` |  |
| 既定収納場所ID | `default_storage_location_id` | `integer` | 識別子または外部キー |
| ギャラリー名前表示 | `gallery_show_name` | `boolean` |  |
| ギャラリータグ表示 | `gallery_show_tags` | `boolean` |  |
| ギャラリー価格表示 | `gallery_show_price` | `boolean` |  |

## ギャラリー保存ビュー（`gallery_view`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 11

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| ギャラリー保存ビューID | `gallery_view_id` | `bigint` | 識別子または外部キー |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| ビュー名 | `view_name` | `text` |  |
| 検索語 | `q` | `text` |  |
| カテゴリタグID一覧 | `category_tag_ids` | `integer[]` |  |
| 収納場所ID一覧 | `storage_location_ids` | `integer[]` |  |
| カラータグスロット一覧 | `color_tag_slots` | `integer[]` |  |
| 一覧並び | `list_sort` | `text` |  |
| 表示順 | `display_order` | `integer` | 表示並び順 |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## アイコンタグ（`icon_tag`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 4

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| アイコン | `icon` | `text` |  |
| アイコン名 | `icon_name` | `text` |  |
| カテゴリータグ使用フラグ | `category_tag_use_flag` | `integer` | 真偽・0/1 フラグ |
| 収納場所使用フラグ | `storage_location_use_flag` | `integer` | 真偽・0/1 フラグ |

## 会員（`member`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 10

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 会員種別名 | `members_type_name` | `text` |  |
| ユーザー名 | `user_name` | `text` |  |
| メールアドレス | `email_address` | `text` |  |
| XID | `x_id` | `text` | 識別子または外部キー |
| InstagramID | `instagram_id` | `text` | 識別子または外部キー |
| LINEID | `line_id` | `text` | 識別子または外部キー |
| 本人確認種別 | `identity_type` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 会員種別（`member_type`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 6

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員種別名 | `members_type_name` | `text` |  |
| サムネイル画質 | `thumbnail_image_quality` | `integer` |  |
| 登録可能数 | `registerable_number` | `integer` |  |
| 高解像度登録可能枚数 | `high_resolution_registerable_number` | `integer` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 推し色設定（`oshi_accent_settings`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 8

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 会員種別名 | `members_type_name` | `text` |  |
| メイン色 | `main_hex` | `text` |  |
| サブ色 | `sub_hex` | `text` |  |
| 有効 | `active` | `boolean` |  |
| プリセット一覧 | `presets` | `jsonb` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 写真（`photo`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 14

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 写真ID | `photo_id` | `integer` | 識別子または外部キー |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 写真テーマ色 | `photo_theme_color` | `integer` |  |
| 前面フラグ | `front_flag` | `integer` | 真偽・0/1 フラグ |
| 写真サムネイル | `photo_thumbnail` | `bytea` |  |
| 写真サムネイル画質 | `photo_thumbnail_image_quality` | `integer` |  |
| 写真高解像度フラグ | `photo_high_resolution_flag` | `integer` | 真偽・0/1 フラグ |
| 写真編集済フラグ | `photo_edited_flag` | `integer` | 真偽・0/1 フラグ |
| 写真登録日 | `photo_registration_date` | `timestamp with time zone` | 日付 |
| 写真編集日 | `photo_edit_date` | `timestamp with time zone` | 日付 |
| 写真サムネイルURL | `photo_thumbnail_url` | `text` | URL またはパス |
| 写真高解像度URL | `photo_high_resolution_url` | `text` | URL またはパス |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 製品サイズ（`product_size`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 8

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 製品サイズID | `product_size_id` | `integer` | 識別子または外部キー |
| 製品グループID | `product_group_id` | `integer` | 識別子または外部キー |
| 製品種別 | `product_type` | `text` |  |
| 製品横サイズ | `product_size_horizontal` | `integer` |  |
| 製品奥行 | `product_size_depth` | `integer` |  |
| 製品縦サイズ | `product_size_vertical` | `integer` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 製品種別（`product_type`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 4

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 製品グループID | `product_group_id` | `integer` | 識別子または外部キー |
| 製品グループ名 | `product_group_name` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 登録製品（`registered_product`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 48

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 登録製品ID | `registered_product_id` | `integer` | 識別子または外部キー |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 写真ID | `photo_id` | `integer` | 識別子または外部キー |
| 作品シリーズID | `works_series_id` | `integer` | 識別子または外部キー |
| 作品ID | `work_id` | `integer` | 識別子または外部キー |
| キャラクターID | `character_id` | `integer` | 識別子または外部キー |
| 版権会社ID | `copyright_company_id` | `integer` | 識別子または外部キー |
| 製品グループID | `product_group_id` | `integer` | 識別子または外部キー |
| 製品サイズID | `product_size_id` | `integer` | 識別子または外部キー |
| 収納場所ID | `storage_location_id` | `integer` | 識別子または外部キー |
| 収納場所タグID | `storage_location_tag_id` | `integer` | 識別子または外部キー |
| カラータグID | `color_tag_id` | `integer` | 識別子または外部キー |
| カテゴリータグID | `category_tag_id` | `integer` | 識別子または外部キー |
| キャンペーンID | `campaign_id` | `integer` | 識別子または外部キー |
| 貨幣単位ID | `currency_unit_id` | `integer` | 識別子または外部キー |
| 作品シリーズ名 | `works_series_name` | `text` |  |
| タイトル | `title` | `text` |  |
| キャラクター名 | `character_name` | `text` |  |
| 版権会社名 | `copyright_company_name` | `text` |  |
| 製品種別 | `product_type` | `text` |  |
| 製品横サイズ | `product_size_horizontal` | `integer` |  |
| 製品奥行 | `product_size_depth` | `integer` |  |
| 製品縦サイズ | `product_size_vertical` | `integer` |  |
| バーコード番号 | `barcode_number` | `text` |  |
| バーコード種別 | `barcode_type` | `text` |  |
| 製品名 | `product_name` | `text` |  |
| 定価 | `list_price` | `integer` |  |
| 購入価格 | `purchase_price` | `integer` |  |
| 登録数量 | `registration_quantity` | `integer` |  |
| 売却希望数量 | `sales_desired_quantity` | `integer` |  |
| 製品シリーズ数量 | `product_series_quantity` | `integer` |  |
| 購入場所 | `purchase_location` | `text` |  |
| おまけ名 | `freebie_name` | `text` |  |
| 購入日 | `purchase_date` | `date` | 日付 |
| 作成日 | `creation_date` | `timestamp with time zone` | 日付 |
| 更新日付 | `updated_date` | `timestamp with time zone` | 日付 |
| その他タグ | `other_tag` | `text` |  |
| メモ | `memo` | `text` |  |
| 製品シリーズフラグ | `product_series_flag` | `integer` | 真偽・0/1 フラグ |
| 市販品フラグ | `commercial_product_flag` | `integer` | 真偽・0/1 フラグ |
| 個人制作フラグ | `personal_product_flag` | `integer` | 真偽・0/1 フラグ |
| デジタル製品フラグ | `digital_product_flag` | `integer` | 真偽・0/1 フラグ |
| 売却希望フラグ | `sales_desired_flag` | `integer` | 真偽・0/1 フラグ |
| 欲しい物フラグ | `want_object_flag` | `integer` | 真偽・0/1 フラグ |
| おまけフラグ | `freebie_flag` | `integer` | 真偽・0/1 フラグ |
| 製品シリーズコンプリートフラグ | `product_series_complete_flag` | `integer` | 真偽・0/1 フラグ |
| 製品グループ名 | `product_group_name` | `text` |  |
| 通貨コード | `currency_code` | `text` |  |

## 登録製品カラータグ（`registered_product_color_tag`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 3

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 登録製品ID | `registered_product_id` | `integer` | 識別子または外部キー |
| スロット | `slot` | `integer` | 枠番号（プリセット等） |

## 収納場所（`storage_location`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 18

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 収納場所ID | `storage_location_id` | `integer` | 識別子または外部キー |
| 収納場所名 | `storage_location_name` | `text` |  |
| 収納場所横サイズ | `storage_location_size_horizontal` | `integer` |  |
| 収納場所奥行 | `storage_location_size_depth` | `integer` |  |
| 収納場所縦サイズ | `storage_location_size_vertical` | `integer` |  |
| 1個あたり収納数 | `storage_count_per_1` | `integer` |  |
| 1個あたり横サイズ | `storage_size_horizontal_per_1` | `integer` |  |
| 1個あたり奥行 | `storage_size_depth_per_1` | `integer` |  |
| 1個あたり縦サイズ | `storage_size_vertical_per_1` | `integer` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 収納場所アイコン | `storage_location_icon` | `text` |  |
| 収納場所使用フラグ | `storage_location_use_flag` | `integer` | 真偽・0/1 フラグ |
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| スロット | `slot` | `integer` | 枠番号（プリセット等） |
| 表示順 | `display_order` | `integer` | 表示並び順 |
| 登録選択回数 | `register_pick_count` | `integer` |  |
| 最終登録選択日時 | `last_register_picked_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 収納場所プリセット削除済（`storage_location_preset_slot_dismissed`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 3

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| スロット | `slot` | `integer` | 枠番号（プリセット等） |
| 却下日時 | `dismissed_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## テーマ設定（`theme_settings`）

- **状態:** アプリ連携済み（API 可）（`wired`）
- **列数:** 5

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 会員ID | `members_id` | `uuid` | テナント（ログインユーザー = auth.uid()） |
| 会員種別名 | `members_type_name` | `text` |  |
| テーマ | `theme` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

## 作品（`work`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 6

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 作品ID | `work_id` | `integer` | 識別子または外部キー |
| タイトル | `title` | `text` |  |
| 作品シリーズID | `works_series_id` | `integer` | 識別子または外部キー |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 版権会社名 | `copyright_company_name` | `text` |  |

## 作品シリーズ（`works_series`）

- **状態:** DB のみ・未連携（Data API 不可）（`schema_ready`）
- **列数:** 4

| 日本語列名 | 物理名 | 型 | 補足 |
|------------|--------|----|------|
| 作品シリーズID | `works_series_id` | `integer` | 識別子または外部キー |
| 作品シリーズ名 | `works_series_name` | `text` |  |
| 作成日時 | `created_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |
| 更新日時 | `updated_at` | `timestamp with time zone` | タイムスタンプ（時刻付き） |

---

未知トークンが残る場合は `docs/db/meta/term_glossary.json` に追加して再生成（エージェント作業。人間は触らない）。
