<!-- 更新: ARCHIVE寄り — 移行メモ。現行正本はルート AGENTS.md と .cursor/rules/*.mdc。凡例: docs/README.md -->
# AGENTS.md（oshi-app v2 草案・追随用）

**エージェントの正本はリポジトリ直下の [`AGENTS.md`](../../AGENTS.md)。**  
ルールは `.cursor/rules/*.mdc`（`alwaysApply` / `globs` 付き）。

このファイルは移行ドキュメント用の入口。食い違う場合はルート `AGENTS.md` と `.mdc` を優先する。

## クイックリンク

| 内容 | パス |
|------|------|
| 入口 | `/AGENTS.md` |
| ルール説明 | `.cursor/rules/README.md` |
| 認証 | `.cursor/rules/auth.mdc` |
| セキュリティ | `.cursor/rules/security.mdc` |
| 命名 | `.cursor/rules/naming.mdc` |
| 公式索引 | `docs/refs/official-links.md` |
