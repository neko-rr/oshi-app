<!-- 更新: 手 — 人が書いて直す。凡例: docs/README.md -->
# DoD: 検索

- [x] `/search` でログインユーザーの製品を名前等で絞り込める
- [x] ギャラリーでも `q` 相当の検索ができる
- [x] `GET /products?q=` が API で使える（未ログインは 401）
- [x] 結果は自分のテナントのみ（JWT + RLS）

後続（Must 外の強化）: ファセット・履歴・サーバー側全文検索の高度化。

関連: `search`
