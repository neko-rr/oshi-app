# Plans（作業計画）

## エージェント向け

- **このフォルダ（直下）**: これからやる **未完了の v2 計画**だけ置く
- **`archive/`**: 完了済み・Dash 期・旧パスの計画（**新規実装の正本にしない**）
- 実装の正本は常に `AGENTS.md` と `.cursor/rules/*.mdc`

新しい計画を足すときは:

1. Next.js / FastAPI（`apps/web`・`apps/api`）前提であること
2. 完了したら `archive/` へ移す（削除してもよい）

現在、直下にアクティブな plan はありません（すべて archive 済み）。
