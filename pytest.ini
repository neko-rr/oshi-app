# pytest（ルート）。monorepo 後は apps/api からも実行する。
[pytest]
testpaths =
    tests
    apps/api/tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    integration: 外部サービスが必要なテスト（既定ではローカル任意）
    unit: 高速な単位テスト
filterwarnings =
    ignore::DeprecationWarning
